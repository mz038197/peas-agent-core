[project]
name = "peas-agent-core"
version = "0.1.0"
description = "PEAS Agent Workshop core — LangChain ReAct agent with workspace, memory, and skills"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "jinja2>=3.1.0",
    "langchain-core>=0.3.0",
    "langchain-openai>=1.1.13",
    "markitdown>=0.1.6",
]

[project.scripts]
peas-agent = "peas_agent.cli:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/peas_agent"]

[tool.pytest.ini_options]
pythonpath = ["src"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
